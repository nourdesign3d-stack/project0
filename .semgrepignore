# Exclusions Semgrep — chaque entrée est justifiée.
# Une exclusion ne doit jamais masquer un problème critique dans du code que nous écrivons.

# Dépendances et artefacts de build : code non écrit ici, non déployé tel quel.
node_modules/
.next/
.turbo/
dist/
build/
coverage/

# Client Prisma généré : régénéré à chaque build, non modifiable à la main.
packages/database/generated/

# Types générés par le CMS.
packages/cms/basehub-types.d.ts

# Les composants shadcn/ui NE sont PAS exclus : ils sont compilés et livrés aux
# utilisateurs, donc analysés comme le reste. Mesuré le 2026-08-05 : 0 constat
# sur 452 cibles. Ils restent exclus du lint Biome (style, 669 écarts, écrasés à
# chaque `pnpm bump-ui`) — la sécurité et le style ne se traitent pas pareil.

# Rapports de test : peuvent contenir des traces, jamais du code applicatif.
playwright-report/
test-results/
