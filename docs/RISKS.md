# Risques

Gravité : `critique` / `élevée` / `moyenne` / `faible`.
Probabilité : `haute` / `moyenne` / `basse`.
Statut : `ouvert` / `atténué` / `accepté` / `fermé`.

| ID | Risque | Gravité | Probabilité | Contrôle en place | Propriétaire | Statut |
| --- | --- | --- | --- | --- | --- | --- |
| R-001 | Aucun modèle métier ni invariant défini : tout code écrit maintenant repose sur des suppositions | élevée | haute | `.claude/rules/project-domain.md` interdit d'inventer ; `ASSUMPTIONS.md` trace les hypothèses | propriétaire produit | ouvert |
| R-002 | `relationMode = "prisma"` : aucune clé étrangère appliquée par la base — intégrité référentielle à la charge de l'application | élevée | haute | règle explicite dans `.claude/rules/database.md` + axe dédié dans `/blind-spot-review` | à désigner | ouvert |
| R-003 | **Aucune protection bot/WAF** : Arcjet retiré (D-014), limitation de débit inactive faute de clé Upstash. Une route publique coûteuse est exposée à l'abus | élevée | haute | aucun contrôle automatique ; bornes à écrire dans le code de chaque route exposée. Les deux webhooks refusent désormais **avant de lire le corps** : celui de Stripe bufferisait la charge utile entière d'un appelant anonyme avant de constater l'absence de signature (corrigé le 2026-08-06, D-039, test qui piège `request.text()`). Reste entier pour toute autre route publique ou coûteuse | à désigner | **ouvert, assumé** |
| R-004 | Aucune **politique** de sauvegarde : fréquence, rétention, emplacement et délai de reprise ne sont pas décidés | critique | moyenne | le **mécanisme** existe et a été **répété le 2026-08-05** (D-027) : `pnpm db:backup` / `pnpm db:restore`, sauvegarde d'une base Neon puis restauration ailleurs, contenu et migrations contrôlés. Procédure et garde-fous dans `docs/RECOVERY.md`, 5 tests de refus. Ce qui manque n'est plus technique : c'est une décision de propriétaire (H-008). ⚠️ La confirmation était **insuffisante pour la cible distante** : `--yes` étant lu dans le même `argv` que `--to database-url`, une ligne collée restaurait sans second geste humain — le scénario que l'en-tête du script prétendait empêcher. Corrigé le 2026-08-06 (D-039) : saisie du nom de la base au terminal, refus hors terminal | propriétaire produit | **partiellement traité — mécanisme éprouvé, politique absente** |
| R-005 | Dérive de versions du template : les dépendances en `^` ont dépassé les API utilisées par next-forge 6.0.3 (Clerk Core 3, AI SDK v6, Knock, Stripe agent toolkit, recharts 3, react-resizable-panels 4) | moyenne | **avérée** | corrections appliquées, `pnpm typecheck` à 0 erreur, Dependabot mensuel groupé | à désigner | atténué |
| R-006 | Corrections de dérive non validées à l'exécution | moyenne | moyenne | vérifiés à l'exécution le 2026-08-05 sur un projet jetable (D-018) : **Clerk** (widget, session, page authentifiée), **Neon** (D-025), **Sentry** (D-026, au collecteur local) **Stripe** en livraison réelle (D-030 : signature émise par Stripe acceptée, forgée refusée, rejeux non retraités) et **BetterStack** (D-028). **BaseHub** a été retiré sans avoir été exécuté (D-031) et le package **IA** est désormais éprouvé contre un point d'accès local (D-032). Restent non exécutés : le rapprochement client Stripe (R-020) et tout Connect (D-029) | à désigner | **partiellement levé** |
| R-007 | `pnpm build` complet impossible sans `BASEHUB_TOKEN` : `apps/web` n'était **pas** couvert par la CI | moyenne | haute | **fermé le 2026-08-05** (D-031) : BaseHub retiré, `apps/web` se construit sans aucun jeton et le build **complet** est entré dans le job `quality` — il tourne à chaque exécution. Le job `build-full` et la variable `ENABLE_FULL_BUILD` n'ont plus d'objet et sont supprimés | à désigner | **fermé** |
| R-008 | Parcours **authentifié** non couvert en CI | moyenne | haute | **fermé le 2026-08-05** : `ENABLE_E2E` est positionnée, les cinq secrets de test sont posés, et le job Playwright joue le parcours complet — proxy, mot de passe, vérification d'appareil, session, organisation, page authentifiée, requête Prisma — **à chaque exécution sur `main`**. Cinq défauts corrigés au passage, dont deux dépassaient la CI (aucune migration versionnée, base illisible à l'exécution) | à désigner | **fermé** |
| R-009 | Surface de dépendances très large (≈20 packages, ~15 services tiers) pour un produit sans périmètre défini | moyenne | haute | hypothèse H-006 : arbitrage de suppression à faire | propriétaire produit | ouvert |
| R-010 | Ce qui part vers Sentry n'avait jamais été observé | élevée | **avérée** | **mesuré le 2026-08-05** sur un collecteur local (D-026) : les **transactions** — une par requête, erreur ou non — emportaient l'en-tête `authorization`, le cookie de session, le corps complet et le jeton d'URL. Le filtre ne couvrait que les erreurs, et le runtime edge n'en avait aucun. Filtre unique appliqué aux deux canaux et aux trois runtimes. ⚠️ **Audit externe du 2026-08-06** : l'expression était **ancrée** en début de chaîne, donc un jeton dans un message d'erreur — `exception.values[].value`, le champ le plus exposé — sortait intact à chaque requête. Corrigé (D-035) : raisonnement mot par mot, cycles traités par `WeakSet`, borne de profondeur portée de 12 à 64. Neuf tests, dont le cas vu échouer avant correction | à désigner | **atténué** |
| R-027 | Les artefacts de CI ne sont **jamais caviardés** par GitHub : une trace Playwright contient en-têtes et corps de requête, donc mot de passe de connexion et cookie de session | élevée | **avérée** | **corrigé le 2026-08-06** (D-037) : trace, vidéo et captures coupées pour le seul parcours authentifié, les parcours anonymes conservant leur valeur de diagnostic. Règle rendue exécutoire par `apps/api/__tests__/e2e-artifacts.test.ts`, vu échouer avant correction. Reste à surveiller : tout nouvel artefact téléversé (rapports de couverture, journaux de build) mérite la même question | à désigner | **atténué** |
| R-025 | **Content-Security-Policy désactivée** dans `packages/security/proxy.ts` (`contentSecurityPolicy: false`), sur les trois apps. Aucun document ne le signalait avant le 2026-08-06 | élevée | **avérée** | héritage du template : next-forge la désactive parce que les valeurs dépendent des fonctionnalités activées. Les autres en-têtes Nosecone sont servis et mesurés (D-034). Une CSP se rédige à partir des origines réellement utilisées — Clerk, Sentry, PostHog, Vercel — et ne peut pas être générique. À écrire avant toute mise en service | à désigner | **ouvert** |
| R-026 | Les en-têtes de `apps/web` et `apps/api` **ne sont couverts par aucun test automatique** : seul `apps/app` l'est, la suite e2e ne démarrant qu'une application | moyenne | moyenne | `e2e/tests/security-headers.spec.ts` couvre `apps/app`, y compris le cas de la redirection qui faisait disparaître les en-têtes. Pour les deux autres, la vérification est **manuelle**, à refaire à chaque changement de proxy — procédure dans D-034 | à désigner | **ouvert** |
| R-024 | Les pages légales sont des **modèles non rédigés** : structure attendue, aucun texte d'engagement | élevée | **avérée** | un bandeau visible le dit sur chaque page — une page légale non rédigée qui **ressemble** à une page rédigée engage sans que personne l'ait voulu. Les liens du widget Clerk, jusque-là cassés, mènent désormais quelque part. À rédiger et **faire relire** avant toute mise en service | propriétaire produit | **ouvert** |
| R-022 | Les **journaux** partent vers des tiers sans filtrage : canal `log` de Sentry (`enableLogs: true`) et BetterStack, qui expédie message **et champs structurés en clair** | moyenne | **avérée** | mesuré au collecteur le 2026-08-05 (D-026, D-028) : les deux canaux fonctionnent et n'ont aucun filtre. Aucun posé, faute de mesure sur leur forme complète. Conséquence : la règle « ne jamais journaliser de donnée sensible » cesse d'être une hygiène locale et devient **structurante** — elle protège un flux vers un tiers. Depuis le 2026-08-07 (D-052), les deux webhooks posent un **identifiant de corrélation** sur la trace Sentry (`webhook.provider`, `webhook.event_id`) — la compensation que R-018 exigeait et qu'aucune frontière n'appliquait —, et un refus de signature ne devient plus un incident Sentry : sans limitation de débit (R-003), tout appelant anonyme pouvait noyer les vraies erreurs sous du bruit qu'il choisit | à désigner | **ouvert** |
| R-023 | Les variables de **journalisation** BetterStack n'existaient pas dans le dépôt : `@logtail/next` se rabattait en silence sur la console, et les deux variables présentes servaient en réalité le produit **Uptime** | moyenne | **avérée** | **corrigé le 2026-08-05** (D-028) : `BETTER_STACK_SOURCE_TOKEN` et `BETTER_STACK_INGESTING_URL` ajoutées, familles distinguées et commentées, 8 tests de garde-fou. Non vérifié : que BetterStack **accepte** l'envoi — mesuré contre un collecteur local, pas contre le service | à désigner | **atténué** |
| R-011 | `main` sans protection **côté serveur** : une fusion sans CI verte restait techniquement possible | moyenne | moyenne | **fermé le 2026-08-07** (D-048) : la contrainte était le plan gratuit sur dépôt privé (`403`) ; le passage en public l'a levée et la règle a été appliquée le jour même — `Lint · Typecheck · Test · Build` et `Semgrep` requis, poussée forcée et suppression interdites, conversations résolues. Deux limites assumées : la **revue n'est pas exigée** (à un seul mainteneur, GitHub interdit d'approuver sa propre PR) et les administrateurs ne sont pas soumis à la règle — sortie de secours en incident, donc une confiance, pas un contrôle. Le hook `pre-push` local subsiste comme refus anticipé, plus comme unique rempart | propriétaire du dépôt | **fermé** |
| R-012 | Webhooks non idempotents : un événement Clerk ou Stripe rejoué est traité plusieurs fois | moyenne | haute | **traité le 2026-08-05** (D-023) : modèle `WebhookEvent` à clé primaire composite `(provider, eventId)` — c'est la contrainte de la base qui décide, pas une vérification applicative. Réservation **avant** traitement, libération en cas d'échec. 8 tests dont le rejeu, la mémoire indisponible et la libération. L'attente d'un « modèle de données » était une erreur d'analyse : l'idempotence est d'infrastructure, pas de domaine. ⚠️ **Audit du 2026-08-07** : une ligne signifiait seulement « vu ». Un processus interrompu **entre** la réservation et la fin — redéploiement, délai dépassé — laissait une réservation que rien ne libérait : le réessai passait pour un doublon et l'événement était perdu, le fournisseur ayant reçu un `200`. Corrigé (D-049) : `completedAt` distingue réservé de terminé, reprise **atomique** après 15 min, table bornée à 30 jours par une tâche planifiée. Les réservations jamais abouties ne sont **pas** purgées — ce sont les seules qui méritent d'être vues | à désigner | **atténué** |
| R-013 | Une route ajoutée hors du layout authentifié est publique : `apps/app/proxy.ts` ne protège rien | élevée | **avérée** | **mesuré le 2026-08-05** : une route `/probe` hors du groupe `(authenticated)` répondait `200` à un anonyme, `userId: null`. Le proxy **reste** volontairement du routage — Clerk 7 déprécie la protection par correspondance de chemins, « qui peut diverger du routage réel et laisser des ressources protégées joignables » (D-019). Deux garde-fous (D-024) : un test **statique** qui exige qu'un contrôle existe (plancher, tourne sans serveur) et un test **d'exécution** qui interroge chaque route sans session et exige un refus. Le second attrape ce que le premier ne voit pas — un `auth()` dont on ignore le résultat : démontré sur une sonde. ⚠️ **Audit externe du 2026-08-06** : le plancher ne tenait pas — il créditait les layouts parents, donc un `route.ts` nu sous `(authenticated)` répondait `200` à un anonyme avec le test au vert, et le test d'exécution transformait toute route dynamique en URL inexistante dont le `404` comptait pour un refus. Corrigé (D-033) : contrôle exigé **dans le fichier de la route**, routes dynamiques déclarées non couvertes et signalées bruyamment | à désigner | **atténué** |
| R-014 | 57 alertes de vulnérabilité dans l'arbre de production | faible | avérée | **triées à l'exécution le 2026-08-07** (D-056), et le résultat change la lecture du risque : ~32 `hono`/`@hono/node-server` viennent de `@prisma/dev` (outillage local, jamais exécuté en production), ~12 `undici` de `@repo/ai` qu'**aucune application n'importe** (D-032), 3 `lodash` de la même chaîne Prisma, 4 `postcss` et 1 `uuid` du temps de build. **Une seule était atteignable en production** — `sharp`, quatre CVE libvips, gravité haute — et elle est **corrigée** : montée en 0.35.3 plus un `override` sur tout l'arbre, `next` embarquant sa propre copie. Mesure : 1 → 0. Reste `valibot` (modérée) via la validation d'environnement au démarrage. Passe mensuelle Dependabot avec délai de 7 jours | à désigner | **atténué** |
| R-015 | `trustPolicy: no-downgrade` a écarté quatre paquets en une semaine : il refuse aussi des correctifs légitimes, et rend `pnpm dedupe` inutilisable sur cet arbre | moyenne | avérée | une exception nommée (`chokidar`) **avec échéance au 2026-11-05** dans `pnpm-workspace.yaml` ; relecture rappelée par `/vibe` ⚠️ **Coût mesuré le 2026-08-07** (D-056), et il est plus lourd qu'annoncé : le dépôt était **gelé** — `pnpm install --frozen-lockfile` passait et la CI était verte, mais `pnpm add` et `pnpm up` échouaient sur `semver@6.3.1`, **déjà présent dans le lockfile**. Aucun correctif de sécurité n'était donc applicable, et rien ne le signalait. Deuxième exception nommée et datée posée (échéance 2026-11-07). Un contrôle qui empêche d'installer un paquet douteux est utile ; un contrôle qui empêche de **corriger** ne protège plus rien : **vérifier périodiquement qu'une montée de version reste possible**, la CI ne le fait pas | à désigner | ouvert |
| R-016 | Le garde-fou Bash **n'est pas une frontière de sécurité** : cinq audits successifs l'ont contourné. Le cinquième (externe, 2026-08-06) a relevé **29 formulations, sans aucun obscurcissement** — des guillemets autour d'un drapeau désarmaient toutes les règles, `~/Documents` échappait au bac à sable, et un heredoc effaçait de l'analyse une commande que le shell exécutait | moyenne | haute | **113 cas de test** après réparation (D-036) : jeton quoté jugé sur son espacement, tous les chemins comptés après expansion du `~`, corps de heredoc borné à partir de la ligne suivante, groupes `( )` et `{ }` séparés, lecteurs et destructions complétés. La limite reste réelle — encodage, variable construite, interpréteur tiers — mais elle n'est plus « il faut être déterminé » : elle est « il faut le vouloir » | à désigner | **accepté, limite requalifiée** |
| R-017 | Résidus d'une copie de dossier : environnements hérités, instance Clerk, caches de build | élevée | moyenne | `git clone` prescrit ; `project:init` supprime toujours les caches (racine **et** par workspace) ; à nom changé il régénère les environnements, à nom égal il **refuse** et explique (`--fresh` / `--keep-env`) ; 4 cas de test dont le refus | à désigner | **atténué** |
| R-020 | Le webhook Stripe retrouve l'utilisateur par **balayage** de tous les comptes Clerk (coût linéaire à chaque événement) : Clerk ne sait pas filtrer sur `privateMetadata` | moyenne | moyenne | pagination correcte et borne explicite à 50 pages, dépassement journalisé (la version précédente ne lisait **que la première page** et échouait en silence). Bonne approche non implémentable ici : inscrire l'identifiant Clerk dans les `metadata` du client Stripe à sa création — rien dans la graine ne crée de client Stripe. ⚠️ **Ce rapprochement devient faux en Connect** (D-029) : les identifiants client appartiennent à un espace par compte connecté, il n'y a pas d'espace unique à balayer.<br>**Comment l'éprouver le jour venu** : créer un client Stripe de test (`stripe customers create`), écrire son identifiant dans le `privateMetadata.stripeCustomerId` d'un utilisateur Clerk de test, puis déclencher un `checkout.session.completed` **rattaché à ce client**. ⚠️ Avec deux comptes, on éprouve la correspondance, **pas la pagination** — le défaut d'origine ne se manifeste qu'au-delà de 100 utilisateurs, soit une page. **Quand** : avant tout usage en production de ce gestionnaire. **Ou jamais**, si Connect (D-029) le remplace d'abord — ce serait alors valider du code destiné à disparaître | à désigner | **ouvert** |
| R-019 | Connexion à Neon non vérifiée : le pilote Postgres standard remplace l'adaptateur Neon (D-021) | moyenne | moyenne | **vérifié sur Neon le 2026-08-05** (D-025) via le point d'accès « pooler », TLS négocié : `migrate status`, `migrate deploy` **et** une requête applicative réelle du client Prisma. Aucun `directUrl` n'a été nécessaire. Reste non exercé : les migrations complexes (verrous consultatifs), que le pooler peut refuser | à désigner | **fermé pour le chemin nominal** |
| R-021 | `sslmode=require` sera **affaibli** par une montée en `pg` 9 : aujourd'hui traité comme `verify-full` (certificat vérifié), il adoptera la sémantique libpq — chiffrer sans vérifier | moyenne | basse | avertissement émis par `pg-connection-string` 2.14 et relevé le 2026-08-05 ; aucun changement de code ne signalera la bascule. Contrôle à poser **avant** d'accepter une PR Dependabot passant `pg` en 9 : exiger `sslmode=verify-full` explicite dans les chaînes de production | à désigner | **ouvert** |
| R-018 | Sentry bridé : sans variables locales ni corps de requête, une erreur de validation n'est plus reproductible depuis Sentry seul | moyenne | moyenne | contrepartie assumée pour ne pas exfiltrer de donnée personnelle ; compensation attendue au cas par cas (identifiant de corrélation + forme de l'erreur, jamais la valeur) — inscrite dans `.claude/rules/security.md`, **non implémentée** faute de frontière métier | à désigner | ouvert |

## Revue

Ce tableau est relu à chaque `/release-readiness` et à chaque `/vibe`. Un risque ne se
ferme que lorsque son contrôle est **vérifié**, pas lorsqu'il est prévu.
